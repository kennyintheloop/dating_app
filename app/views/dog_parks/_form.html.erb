<%= form_for(@dog_park) do |f| %>
  <% if @dog_park.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dog_park.errors.count, "error") %> prohibited this dog_park from being saved:</h2>

      <ul>
      <% @dog_park.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :addr %><br />
    <%= f.text_field :addr %>
  </div>
  <div class="field">
    <%= f.label :lati %><br />
    <%= f.text_field :lati %>
  </div>
  <div class="field">
    <%= f.label :longi %><br />
    <%= f.text_field :longi %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
