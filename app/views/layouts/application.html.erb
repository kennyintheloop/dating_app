<!DOCTYPE html>
<html>
<head>
  <title>DatingApp</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= yield :head %>
</head>
<body>
<div class="container">
<p><%= notice %></p>
<center>
<div id="user_nav">
  <div class="row demo-row">
    <div class="span3"></div>
    <div class="span6">
    <% if current_user %>
      <center><h1> Signed in as <strong><%= current_user.name %></strong>!</h1></center>
      <%= link_to "Sign out", signout_path, id: "sign_out", class: "btn btn-large btn-block btn-primary" %>
    <% else %>
      <%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in", class: "btn btn-large btn-block btn-primary"  %>
    <% end %>
    </div>
    <div class="span3"></div>
  </div>
</div>
<nav>
    <%= link_to "Main page", user_details_url %> |
    <%= link_to "Check out pets", pet_details_url %> |
    <%= link_to "Photos", photo_albums_url %> |
    <%= link_to "Videos", videos_url %> |
    <%= link_to "Dog Parks", dog_parks_url %>
  </nav>
  </center>
<%= yield %>
</div>

<%= yield :scripts %>
</body>
</html>
